<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- Permissions for Foreground Service and Battery Monitoring -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Permissions for AlarmManager (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="E-Education"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EEducationApp"
        tools:targetApi="31">

        <!-- FileProvider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- Main Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Materi Activity -->
        <activity
            android:name=".ui.materi.MateriActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Materi Detail Activity -->
        <activity
            android:name=".ui.materi.MateriDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.materi.MateriActivity" />

        <!-- News Activity -->
        <activity
            android:name=".ui.news.NewsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="Pengaturan" />

        <!-- Study Tracker Activity -->
        <activity
            android:name=".ui.studytracker.StudyTrackerActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="Study Tracker" />

        <!-- Study Timer Service -->
        <service
            android:name=".background.service.StudyTimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Study timer for tracking learning sessions" />
        </service>

        <!-- Battery Level Receiver -->
        <receiver
            android:name=".background.receiver.BatteryLevelReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BATTERY_CHANGED" />
                <action android:name="android.intent.action.BATTERY_LOW" />
            </intent-filter>
        </receiver>

        <!-- Study Reminder Receiver -->
        <receiver
            android:name=".background.receiver.StudyReminderReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Materi Detail Activity -->
        <activity
            android:name=".MateriDetailActivity"
            android:exported="false"
            android:parentActivityName=".MateriActivity" />

    </application>

</manifest>